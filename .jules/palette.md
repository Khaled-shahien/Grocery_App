# Palette 🎨 Learning Journal

- Found that `IconButton`s in the application were missing `tooltip` semantics, severely degrading the experience for screen-reader users and making the UI harder to understand without text labels.
- Critical learning: Always provide a `tooltip` or semantic label on `IconButton` widgets if they contain only icons.
